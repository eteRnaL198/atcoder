"use strict";
const main = (input) => {
  input = input.trim().split("\n");
  const str = input[1].split("");
  str.reduce((val, idx, acc) => {
    
  }, str);
}

main(require("fs").readFileSync("/dev/stdin", "utf8"));